# AgenticASR 当前精修规则与实现说明

> 更新日期：2026-09-16
> 当前说明以浏览器 Web 与 system.live_qwen_refiner 代码路径为范围。其他入口可能有不同的调度和记录字段；跨入口一致性仍以当前项目状态文档中的待办为准。confidence 若未校准，不应用作准确率概率解释。
>
> 适用范围：当前浏览器 WebSocket 服务与 `system.live_qwen_refiner` 实时客户端。
> 本文以当前源码为准，不代表原始 Refiner 训练数据的旧协议。

## 1. 精修不是一个单独模型

当前“精修”是一条由确定性规则、术语保护、神经网络 Refiner、语义校验和失败回退共同组成的处理链。

```text
音频
  ↓
Qwen3-ASR 累积转录
  ↓
句子分块与滑动窗口
  ↓
实体保护：把术语、URL、标识符、缩写和数字成语替换为 __ENTITY_NNN__
（普通数字不占位）
  ↓
确定性数字转换：五个 → 5个、一百二十三万 → 1230000
  ↓
保守门控：调用 Refiner 或跳过模型
  ↓
Refiner 最小修改（看到的是含占位符、已完成数字转换的文本）
  ↓
占位符数量/顺序校验并恢复原值
  ↓
数值校验 + 语义完整性校验
  ↓
失败：本分段回退到规则处理后的 ASR 原文
成功：接受模型候选
  ↓
跨分段确定性去重
  ↓
最终 clean_text
```

原始转录始终保存在 `raw_text`。规则、模型和回退只改变 `clean_text`。

### 1.1 占位符具体用在哪里

占位符只用于保护不希望 Refiner 自由改写的文本片段。进入 Refiner 前，系统会把以下内容临时替换为 `__ENTITY_000__`、`__ENTITY_001__` 等：

- 实体库中精确命中的标准名称或别名；
- 达到自动规范化条件的低风险模糊术语；
- 会话记忆中已经可信的实体；
- URL、电子邮箱、含字母的标识符和大写缩写；
- `一五一十`、`三心二意` 等固定数字成语。

当前 Web 和实时路径创建 `EntityProtector` 时明确设置 `protect_numeric_spans=False`，因此普通中文数字、阿拉伯数字、日期、时间和金额不放入占位符。它们由确定性数字规则直接转换，再以转换后的真实字符交给 Refiner。

例如：

```text
原文：我用Qwen3-ASR一五一十地说明，再给他五个苹果。

实体保护后：
我用__ENTITY_000____ENTITY_001__地说明，再给他五个苹果。

数字规则处理后，也是 Refiner 实际看到的正文：
我用__ENTITY_000____ENTITY_001__地说明，再给他5个苹果。

Refiner 通过校验并恢复占位符后：
我用Qwen3-ASR一五一十地说明，再给他5个苹果。
```

模型输出后，系统先检查每个占位符是否恰好出现一次、ID 是否有效、顺序是否改变。检查通过才按记录恢复原文或实体标准名；检查失败则拒绝该候选并回退。数字虽然不占位，但会单独比较转换前后的数值及顺序，所以模型把 `5` 删除或改成 `10` 同样不能通过。

如果门控决定跳过 Refiner，系统不会把带占位符的文本直接输出，而是恢复占位符后继续执行数字规则和确定性去重。

## 2. 规则归属总表

| 能力 | 实现方式 | 是否调用模型 | 主要代码 |
| --- | --- | --- | --- |
| 中文数字转换 | 确定性规则 | 否 | `system/numeric_normalizer.py` |
| 连续相同短句去重 | 确定性规则 | 否 | `system/deterministic_cleanup.py` |
| `你你`、`我我`等口吃去重 | 确定性白名单 | 否 | `system/deterministic_cleanup.py` |
| 成语数字保持中文 | 固定短语列表 + 占位符 | 否 | `system/numeric_normalizer.py`, `system/protection.py` |
| 精确术语与别名规范化 | 实体库与确定性恢复 | 否 | `system/entity_store.py`, `system/protection.py` |
| 模糊术语候选 | 字符、拼音、长度和领域综合评分 | 可能 | `system/entity_matcher.py` |
| 去口癖、上下文错字、标点 | Refiner 模型 | 是 | `system/live_qwen_refiner.py` |
| 自我修正 | 分块规则 + Refiner + 校验 | 是 | `system/chunking.py`, `system/refinement_guard.py` |
| 内容删减、篡改和幻觉检测 | 确定性对齐与数值比较 | 否 | `system/refinement_guard.py` |
| 结构化局部补丁 | JSON keep/replace、唯一 source 定位、非法补丁回退 | 是 | `system/refinement_protocol.py`, `system/web_app.py` |
| 失败重试与回退 | 严格提示词 + 分段回退 | 第一次失败时重试 | `system/entity_pipeline.py`, `system/web_app.py` |

## 3. ASR 分块与滑动窗口

### 3.1 句子分块

默认兼容路径的 `ChunkManager` 优先在 `。！？.!?` 后断句；超过长度上限时，再尝试在逗号、分号、冒号或顿号处切分。

Web 的 `tri_state` 路径使用更短的单标点策略：逗号、分号、冒号和句末标点会结束当前窗口，标点保留在当前窗口内；顿号连接的并列成分留在同一窗口，原文顿号仍参与标点完整性校验。活动模型窗口以连续 3 个标点块为一组（仍受 80 字符上限约束）。数字之间的小数点（如 `4.5`）不会结束窗口。没有分块标点的超长文本仍按 80 字符上限兜底。

在 `tri_state` 路径中，若窗口仅因数字规范化进入 Refiner、没有实体提示或其他清理信号，验收时保留原始窗口末尾标点，避免数字转换顺带删掉跨窗口的逗号或句号；窗口内部的标点仍可改动。带有口吃、自我修正、断句异常等其他清理信号的窗口继续允许模型修改末尾标点，后续仍经过常规内容校验。没有结束标点的片段不会凭空补标点。

Refiner 请求优先要求返回单个 JSON 局部补丁：`keep` 表示保留原文，`replace` 必须提供输入中唯一存在的连续 `source`、`target` 和修改原因。旧模型返回普通文本时仍兼容，但会继续经过完整校验；JSON 解析失败或 source 不唯一时直接回退原文。

兼容路径窗口参数：

- 最多保留 3 个最新句块；
- 活动窗口最多 80 个字符；
- 旧句块离开活动窗口后成为已提交内容；
- ASR 修订早期假设时，依据原始句块相等性使缓存失效，不使用精修后的字符偏移；
- 最终阶段不复用已经被拒绝的中间精修结果。

### 3.2 自我修正分块

当句子包含 `不对`、`不是`、`而是`、`应该是` 时，系统尽量把错误片段与修正片段放在同一句块中。

在 tri_state 的“一个标点一个块”模式下，自我修正有一个专门例外：当修正
标记落在独立块中，系统会临时回收前一块，并带上后续修正块，组成一个逻辑
自我修正块后再调用 Refiner。这样 `错误内容，不对，修正内容。` 不会因为
逗号把错误内容提前提交。该合并只影响精修归属，不改变 `raw_text`，普通
标点块仍保持原有的三块滑动窗口。

```text
我有一个苹果。不对，我有一个梨。
```

不会过早把“一个苹果”当作已确定结果提交。一个活动窗口包含多个自我修正句块时，会分别精修，避免一次生成压缩掉多个修正关系。

## 4. 确定性数字转换

### 4.1 当前顺序

当前 Web 和实时客户端设置 `protect_numeric_spans=False`。数字不再变成 `__ENTITY_NNN__` 占位符。

```text
我让他五个法器；再不行，我让他十个。
  ↓ 规则先转换
我让他5个法器；再不行，我让他10个。
  ↓ 直接交给 Refiner
校验输出中的有序数值仍为 [5, 10]
```

模型删除、增加、调换或改写数值时，该候选被拒绝，最终使用规则转换后的原文。

### 4.2 会转换的类型

| 类型 | 输入 | 输出 |
| --- | --- | --- |
| 百分比 | `百分之三十七` | `37%` |
| 完整日期 | `二零一五年十二月五日` | `2015年12月5日` |
| 年月 | `二零一五年十二月` | `2015年12月` |
| 四位数年份 | `二零一五年` | `2015年` |
| 完整时间 | `七点十分` | `7点10分` |
| 金额 | `两千一百三十五元` | `2135元` |
| 计量 | `三十七度` | `37度` |
| 小数范围 | `一到两点五米` | `1到2.5米` |
| 量词数量 | `五个苹果` | `5个苹果` |
| 无尾随单位的位值数 | `二十三` | `23` |
| 无金额单位的大数 | `一百二十三万` | `1230000` |

数字解析支持 `零/〇`、`两`、`十/百/千`、`万/亿` 和 `点`小数。

### 4.3 支持的常用量词

```text
个 只 件 本 张 台 条 位 名 辆 套 箱 袋 杯 份 枚 颗 粒
栋 间 户 艘 架 门 次
```

数字规范化使用上面的常用量词集合；叠词保护使用独立的广覆盖量词
词表 `system/quantifiers.py`，并以“限定词 + 任意汉字 AA”作结构兜底。
词表覆盖名量词、集合量词、容器量词、成形量词、时间/动量词、度量词和
复合量词中的常见形式（例如 `朵、层、艘、串、群、阵、缕、排、束、簇、
波、轮、行、卷、摞、泓、汪` 等），不会把这些量词全部无条件转换成数字。

量词分类和候选清单参考了公开的现代汉语量词分类资料（[汉语量词](https://zh.wikipedia.org/wiki/汉语量词)、[现代汉语量词与名词的子类划分](https://www.tigernt.com/yyy23.php)）。这些资料也说明不同词典的量词收录范围并不完全一致，因此代码保留结构兜底而不是声称存在一个封闭的“全部量词表”。

### 4.4 支持的计量和金额单位

```text
平方公里 平方米 立方米 公里 千米 厘米 毫米 公斤 千克 毫升
小时 分钟 人民币 块钱 平米 米 克 吨 升 秒 度 岁 元 块
```

### 4.5 不会转换的歧义数字

| 输入 | 结果 | 原因 |
| --- | --- | --- |
| `二三个人` | 保持 | 可能表示“两三个人” |
| `二三十个人` | 保持 | 可能表示“二三十人” |
| `十几个苹果` | 保持 | 近似数量 |
| `二十多人` | 保持 | 近似数量 |
| `二三` | 保持 | 无法确认是 `23` 还是列举/概数 |
| `一三八零……` | 保持 | 连续数字可能是电话、编号或逐位读法 |

含有 `几`、`多`、`来`、`余` 等模糊后缀时，优先保持原文。

### 4.6 成语和固定表达保护

以下表达中的汉字数字不转换：

```text
一五一十 一心一意 三心二意 不三不四 乱七八糟 七上八下
五湖四海 四面八方 九牛一毛 十全十美 百里挑一 千方百计
万无一失 一举两得 三番五次 一清二楚 说一不二 十有八九
七七八八 三三两两 五花八门 五颜六色 千言万语 千军万马
千变万化 千山万水 万水千山 九死一生 八九不离十 四通八达
四平八稳 两全其美 两肋插刀 一针一线 一朝一夕 一刀两断
一穷二白 一来二去 一干二净 一百年大计 一百年好合 一千年一遇
百年大计 百年好合 千年一遇
```

固定表达判定优先于广义位值数字规则。因此：

```text
我一五一十地告诉你，让他五个法器。
→ 我一五一十地告诉你，让他5个法器。
```

## 5. 确定性去重

实现文件：`system/deterministic_cleanup.py`。去重在分段精修结果合并后运行，所以可以处理被 ASR 切成多个句块的重复。

### 5.1 连续相同短句

当两个或更多相邻句子满足以下条件时，只保留第一次：

- 去掉句末标点和空白后内容完全相同；
- 必须连续相邻；
- 每句不超过 8 个可见字符；
- 句子边界支持 `。！？.!?；;` 和换行；
- 第二次起全部删除，包括原本可能表示强调的重复。

```text
可恶！可恶！             → 可恶！
可恶！可恶！可恶！       → 可恶！
可恶！可恶！可恶         → 可恶！
快跑！快跑！             → 快跑！
可恶！快跑！可恶！       → 保持原样（不相邻）
```

逗号内部的词语重复，如 `可恶，可恶`，不属于该跨句确定性规则，仍由门控和 Refiner 处理。

### 5.2 逗号分隔短语去重

逗号分隔的连续相同短语也执行确定性去重。短语必须占据完整的逗号字段、内容完全相同、连续出现至少两次，且每项不超过 8 个可见字符：

```text
不，不，不，不，我赔。   → 不，我赔。
少主，少主，少主！       → 少主！
他说少主，少主快走。     → 保持原样（两个逗号字段不相同）
```

### 5.3 句内代词口吃

对以下字符连续出现两次或更多次的情况，只保留一次：

```text
我 你 您 他 她 它 这 那
```

通用汉字 `AA` 不再自动压缩。凡是“数词/限定词 + 量词重叠”的结构，
例如 `一根根`、`一条条`、`一座座`、`一道道`、`一代代`、`一朵朵`、
`一层层`、`一本本`、`一艘艘`、`一串串`、`一群群`、`一阵阵`、`一缕缕`、
`一排排`、`一束束`、`一簇簇`、`一波波`、`一轮轮`，都会被确定性保护；
`源源不断`、`生生不息` 等无数词缀的固定叠词也必须保留。Refiner 若删掉
其中一个重叠字，语义校验会以 `semantic_content_loss` 拒绝候选并回退。

```text
你你竟结成了元婴。 → 你竟结成了元婴。
我我不知道。       → 我不知道。
这这个不对。       → 这个不对。
```

为避免破坏正常汉语叠词，不对其他字做通用的“相同字删除”：

```text
人人平等 天天向上 好好学习 让我看看
→ 全部保持
```

### 5.4 已审核的字幕错断修复

实现文件：`system/transcript_repairs.py`。对于当前样本中确认、且上下文
唯一的字幕残留，使用精确短语补丁，不做任意的全局句号删除：

```text
华夏。在文明                   → 华夏文明
滔滔。香水的背后               → 滔滔江水的背后
最大径流量。更是               → 最大径流量更是
一旦决。口便会                 → 一旦决口，便会
黄河三角洲国家级自然保护区。守护着 → 黄河三角洲国家级自然保护区，守护着
过过度放牧                     → 过度放牧
遍布全舰的自动化设备           → 遍布全线的自动化设备
黄土高原已早已换了模样         → 黄土高原早已换了模样
```

“滔滔江水”“一座渡槽”“没有水库能超越”等需要补充实词的修复，也只在
完整短语完全匹配时执行；未知错断仍交给结构化 Refiner 和语义校验，避免
把普通句号误删或凭空扩写。

这些审核短语在窗口结果合并后再次执行。因此，即使“一个标点一个块”把
错误句号或问号拆成两个块，且三态门控分别将它们判为 `KEEP`，跨块短语仍
能被精确修复。该步骤只改变 `clean_text`，不会改写 `raw_text`，也不需要
新增实体或放宽模糊实体匹配。

### 5.5 延迟

该步骤是编译后正则和线性字符扫描，不调用模型、GPU 或网络。当前机器的微基准：

| 文本长度 | 单次平均耗时 |
| --- | ---: |
| 约 400 字 | 约 0.31 ms |
| 约 4400 字 | 约 3.29 ms |

相对于数秒到十几秒的 Refiner 生成，该开销可忽略。

## 6. 实体、术语和特殊符号保护

### 6.1 当前使用占位符的内容

Web 路径中，数字、阿拉伯数字日期和时间不使用占位符。以下内容仍可能被替换成 `__ENTITY_000__`：

- 实体库中的精确标准名称和别名；
- 会自动规范化的高置信模糊术语；
- URL；
- 电子邮箱；
- 含字母的标识符，如 `Qwen3-ASR`；
- 大写缩写，如 `SPA`；
- 数字成语和固定表达。

数据库实体的 `normalization_policy` 决定恢复时是保留原文还是替换为标准名。

### 6.2 占位符完整性

每个占位符必须：

- 恰好出现一次；
- ID 不能未知；
- 顺序不能变化；
- 相邻实体原本的句子边界不能被合并掉。

系统可以安全修复下划线外框损坏：

```text
ENTITY_000
ENTITY_000__
__ENTITY_000_
→ __ENTITY_000__
```

系统不会猜测丢失的 ID，也不会恢复完全被删除的占位符。

### 6.3 `<KEY>` 术语提示

候选术语最多取 16 个，以下列形式追加到模型输入：

```text
<KEY>[术语1、术语2]
```

`<KEY>` 只是输入术语表，不是用户文本。模型回显的尾部 `<KEY>[...]` 会被删除。候选术语不能凭空出现；新增标准名必须对应地消耗原文中实际出现的别名或候选跨度。

### 6.4 模糊实体匹配

当前运行模式为 `auto`。评分权重：

| 特征 | 权重 |
| --- | ---: |
| 拼音相似度 | 0.55 |
| 字符相似度 | 0.25 |
| 长度相容度 | 0.10 |
| 领域匹配 | 0.08 |
| 实体优先级 | 0.02 |

自动规范化同时要求：

- 总分不低于 0.80；
- 拼音分不低于 0.88；
- 领先第二候选至少 0.12；
- 实体策略必须是 `normalize`；
- 只自动处理 `TERM`、`PROJECT`、`MODEL`；
- `PERSON`、`ORG` 属于高风险类型，不模糊自动替换；
- 中间和最终窗口都允许达标候选进入 `AUTO_NORMALIZE`；如果 ASR 后续改写该
  source span，窗口缓存会按原文归属失效并重新匹配。

模式含义：

| 模式 | 行为 |
| --- | --- |
| `off` | 关闭模糊匹配 |
| `shadow` | 只记录候选，不影响文本 |
| `hint` | 可将候选作为 Refiner 提示，不直接自动替换 |
| `auto` | 每个窗口对达标且低风险的候选自动规范化；最终阶段仍会做一次幂等复核 |

### 6.5 会话实体记忆

单次会话记忆默认上限 128 条，TTL 为 20 分钟。未验证观测需要至少两次独立出现，且平均置信度不低于 0.8，才能升级为可信记忆。已验证实体立即可信。

## 7. Refiner 模型规则

### 7.1 普通提示词约束

当前模型被要求：

- 保留原意，只做最小修改；
- 去除明确的口癖和重复；
- 修正错字；
- 补充必要标点；
- 处理明确的自我修正；
- 不总结、不扩写、不解释；
- 不自行转换数字；
- 不改写数字成语；
- 除明确口头重复和自我修正废弃片段外，不删减信息；
- 人称、指代、否定、数量、动作对象和地点必须保留；
- 无法确定时保留原文；
- 每个 `__ENTITY_NNN__` 必须原样保留一次，不能删除、改写、重复或调换顺序。

### 7.2 生成参数

当前 Web 服务：

- `do_sample=False`，使用贪心/确定性生成；
- `max_new_tokens=256`；
- 单次生成最长时间 12 秒；
- 使用单一 Refiner 锁，避免同一 GPU 并发生成冲突；
- 等待 Refiner 锁最多 30 秒，超时则回退 `refiner_busy`；
- 最终精修使用长度相关截止时间，全局上限 300 秒。

## 8. 精修门控

当前服务支持三种模式：

```text
--refinement-gate-mode off
--refinement-gate-mode conservative
--refinement-gate-mode tri_state
```

`tri_state` 是本次已接入的流式门控模式。它对跨次 ASR 假设使用 `HypothesisTracker`，并把中间事件标记为：

| 状态 | 含义 | 典型条件 |
|---|---|---|
| `KEEP` | 保留当前基线，不调用 Refiner | 稳定、短片段，或完整高置信且无清理信号 |
| `DEFER` | 暂不调用，等待下一次假设 | 文本尾部太新、仍在改写或处于冷却期 |
| `REFINE` | 调用 Refiner | 实体提示、口癖/重复/自我修正、跨块句号、低置信或未结束片段 |

最终事件不允许停留在 `DEFER`。Web 的 `tri_state` 对未校准且没有明确问题信号的完整片段采用 `KEEP`（`confidence_unverified_safe_keep`），避免把不完整的后缀分数当作整段置信度；句子未结束、跨块疑似错断或存在实体/清理信号时仍会 `REFINE`。浏览器事件会附带 `refinement_gate_action`、`refinement_gate_reasons` 和 `refinement_hypothesis`，前端状态栏可直接观察三态变化。

当前 Web 服务默认使用：

```text
--refinement-gate-mode tri_state
```

### 8.1 直接调用模型的情况

按以下顺序判定，任一条先命中即确定路由：

1. 有实体候选提示：`entity_hint_present`；
2. 有可清理信号：`cleanup_signal_present`；
3. 跨三个标点块出现疑似连续句错断：`cross_clause_punctuation`；
4. 置信度已校准且低于 0.80：`confidence_below_refine_threshold`；
5. 句子没有以句末标点结束：`sentence_incomplete`；
6. 在 `conservative` 或显式兼容配置中，置信度缺失/未校准/未覆盖完整句块也会触发 Refiner；`tri_state` Web 默认不因这些字段单独触发。

当前 Qwen 流式置信度日志通常标记为“未校准、只覆盖生成后缀”，因此 `tri_state` 会把它视为未知，并在无其他信号时安全保留基线；不能仅看到 `asr_confidence > 0.92` 就认为一定跳过模型。

### 8.2 直接跳过模型的情况

- 空文本；
- 没有清理信号且可见字符数不超过 2；
- `tri_state` 中完整、稳定且没有明确问题信号的未校准片段（`confidence_unverified_safe_keep`）；
- 或者同时满足：置信度已校准、覆盖完整句块、分数不低于 0.92、句子完整、无清理信号、无术语提示。

跳过 Refiner 后仍会执行数字转换、实体规范化和确定性去重。

### 8.3 清理信号

门控检测：

- 连续重复字符；
- 2–8 字中文短语重复；
- `嗯`、`呃`、`额`、`啊`、`哎`、`呀`、`嘿`、`呼`、`喂`、`这个`、`那个`、`就是` 等独立口癖；
- 嵌入句中的 `嗯`、`呃`，但保护 `嗯哼` 和医学词 `呃逆`；
- 三块活动窗口中的跨句号边界，用于触发一次局部断句检查；
- `不对`、`我是说`、`应该是`、`准确地说`、`更正一下` 等自我修正标记；
- 中文百分比、日期、金额等需要数字规范化的表达；
- 双空格、双逗号、双句号等格式异常。

## 9. 模型输出安全校验

Refiner 输出不会直接进入 `clean_text`。

### 9.1 主要拒绝原因

| 日志原因 | 含义 |
| --- | --- |
| `empty_refiner_output` | 模型输出为空 |
| `empty_or_metadata_output` | 只输出了 `<KEY>` 等元数据，或格式不完整 |
| `placeholder_count:...` | 占位符丢失或重复 |
| `unknown_placeholders:...` | 模型生成未知占位符 |
| `placeholder_order_changed` | 占位符顺序改变 |
| `malformed_placeholder` | 占位符无法安全修复 |
| `entity_sentence_boundary_lost` | 含实体的独立句子被错误合并 |
| `introduced_entity_from_hint:...` | 模型从术语表凭空增加实体 |
| `repeated_sentence` | 模型额外重复生成长句 |
| `repeated_phrase` | 模型陷入额外短语循环 |
| `severe_content_loss` | 输出长度或全局相似度显示大量内容丢失 |
| `numeric_value_mismatch` | 有序数值或单位上下文改变 |
| `semantic_content_loss` | 局部有意义文本被删除 |
| `semantic_substitution` | 关键内容字被替换为功能词，如 `傻 → 的` |
| `unsupported_insertion` | 插入了没有原文依据的重复内容 |
| `truncated_refiner_output` | 完整长文的候选未完整结束且明显变短 |
| `refiner_busy` | 等待单 GPU Refiner 锁超时 |
| `final_refinement_timeout` | 最终精修总截止时间到期 |
| `final_refinement_error:<Type>` | 模型或运行时异常 |

### 9.2 长度和相似度阈值

当前主要阈值：

- 原文至少 16 字且候选长度低于原文的 65%，结合相似度判定严重丢失；
- 较短句子全局相似度低于 0.70 时也可以触发严重丢失；
- 长文原本有完整句末、候选没有完整句末、原文至少 100 字且候选低于 80% 时，判定截断。

数字纯表面转换例如 `五个 → 5个`不参与普通语义删减判定。

### 9.3 局部语义对齐

系统用字符级对齐查看 `delete / replace / insert`：

- 允许标点和空白变化；
- 允许明确口癖字符删除；
- 允许明确重复片段删除；
- 允许删除自我修正的废弃部分，但必须保留修正后尾句；
- 多字有意义文本的普通删除触发 `semantic_content_loss`；
- 人称、否定和指代等单字锚点也优先保护；
- 叠词/重叠量词删除触发 `semantic_content_loss`；
- 一个内容字在句末被换成功能词时，触发 `semantic_substitution`；
- 任意没有原文依据的多字实词插入触发 `unsupported_insertion`；
- 把附近的重复字串复制到新位置时，同样触发 `unsupported_insertion`。

例如：

```text
你当我傻？ → 你当我的？
```

会被拒绝为 `semantic_substitution`。

```text
把他给我找出来 → 把我找出来
```

会被拒绝为 `semantic_content_loss`。

## 10. 重试与回退

以下完整性错误可触发一次更严格的重试：

```text
占位符错误
severe_content_loss
truncated_refiner_output
entity_sentence_boundary_lost
numeric_value_mismatch
semantic_content_loss
semantic_substitution
unsupported_insertion
```

重试提示词要求模型：

- 完整保留每个句子和信息；
- 不总结、不缩写、不合并、不删除；
- 先复制所有占位符，再修正其他文字。

如果重试仍失败，只回退当前句块，其他通过校验的句块仍保留精修结果。回退文本仍会执行：

- 数字确定性转换；
- 已验证实体规范化；
- 跨句和句内确定性去重。

`refiner_accepted=false` 只表示至少一个候选被拒绝，不表示 ASR 或 WebSocket 服务崩溃。

## 11. 当前服务生效配置

```text
ASR: Qwen3-ASR-1.7B
ASR 端口: 8766
Web 端口: 8081
Refiner 设备: cuda:0
Refiner max_new_tokens: 256
实体库: data/entities.db
实体模糊模式: auto
精修门控: tri_state（Web 默认）
规则保护: 开启
数字规范化: 开启
数字占位符: 关闭
```

主要开关：

| 参数 | 作用 |
| --- | --- |
| `--refinement-gate-mode off` | 不使用保守跳过规则，所有非空句块都尝试精修 |
| `--refinement-gate-mode conservative` | 启用兼容的保守门控 |
| `--refinement-gate-mode tri_state` | 启用 KEEP / DEFER / REFINE 与单标点窗口（Web 默认） |
| `--disable-numeric-normalization` | 关闭确定性数字转换 |
| `--disable-rule-protection` | 关闭 URL、邮箱、标识符、缩写和成语等自动占位符保护；数据库实体仍可保护 |
| `--entity-fuzzy-mode off/shadow/hint/auto` | 控制模糊术语候选的使用方式 |

## 12. 日志字段

| 字段 | 含义 |
| --- | --- |
| `raw_text` | ASR 完整原文 |
| `clean_text` | 规则、模型、校验和去重后的最终文本 |
| `refiner_executed` | 是否至少调用过一次 Refiner |
| `refiner_accepted` | 所有句块的最终候选是否都通过 |
| `refiner_masked_outputs` | 模型实际输出的候选，可能包含占位符 |
| `refiner_reject_reasons` | 重试后仍被拒绝的原因 |
| `refiner_retry_count` | 严格重试次数；流式累计值可大于 1 |
| `refiner_retry_reasons` | 初次候选触发重试的原因 |
| `placeholder_retry_count` | 由占位符问题触发的重试数 |
| `refinement_gate_decisions` | 每个句块的 `refine/skip` 判定和信号 |
| `refinement_gate_skipped_segments` | 跳过模型的句块数 |
| `numeric_normalizations` | 规则数字转换明细 |
| `protected_entities` | 占位符保护明细 |
| `entity_candidates` | 模糊实体候选、分数和决策 |
| `entity_normalizations` | 实际完成的实体标准化 |
| `entity_audit_issues` | 最终文本中可信实体的缺失问题 |
| `asr_confidence_metadata` | 置信度是否校准、是否覆盖全文及数据来源 |
| `refiner_session_stats` | 会话内模型调用、重试、延迟、跳过和繁忙计数 |

## 13. 已知边界

1. Refiner 是生成式模型，提示词不能从数学上保证不删词或不改词；因此必须保留校验和回退。
2. 精修只看文本，不重听音频。`丹后期`、`楼主使不得` 等 ASR 已经遗失的语音信息无法靠保守规则可靠恢复。
3. 当模型候选被拒绝时，原始 ASR 错误可能因回退而留在 `clean_text`；这是“宁可保留原文，不接受语义篡改”的取舍。
4. 通用的叠词不做全局删重，否则会破坏 `人人`、`好好`、`看看`、`谢谢`等正常汉语。
5. 跨句确定性去重会删除两次相同短句中的第二次，包括有意的情绪强调；这是当前明确采用的产品规则。
6. 模型训练时使用的旧协议曾要求模型自行规范数字并输出 `<KEY>`；当前运行时协议已改为规则处理数字和 `<KEY>` 仅作输入提示。因此日志中仍可能看到模型回显 `<KEY>`，代码会删除这些元数据。

## 14. 修改规则时必须同步的测试

| 规则 | 测试文件 |
| --- | --- |
| 数字转换 | `tests/test_numeric_normalizer.py`, `tests/test_refinement_guard.py` |
| 数字不使用占位符 | `tests/test_entity_protection.py`, `tests/test_web_app_finish.py` |
| 去重 | `tests/test_deterministic_cleanup.py`, `tests/test_window_refinement.py` |
| 占位符与实体 | `tests/test_entity_protection.py`, `tests/test_entity_matcher.py` |
| 门控 | `tests/test_refinement_gate.py` |
| 语义安全校验 | `tests/test_refinement_guard.py` |
| WebSocket 最终输出、重试和回退 | `tests/test_web_app_finish.py` |
| 流式窗口与缓存 | `tests/test_window_refinement.py` |

当前主项目和实际运行目录的完整测试基线均为：

```text
212 passed
```
