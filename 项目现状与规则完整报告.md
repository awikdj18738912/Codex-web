# AgenticASR 项目现状与规则完整报告

> 生成日期：2026-09-21
>
> 本报告基于当前仓库代码、规则实现、启动脚本、实验管线、测试结果、运行日志和 Git 工作区状态整理。

## 一、项目定位

AgenticASR 不是 ASR 本身，而是 ASR 后处理系统。

ASR 负责：

~~~text
音频 → 原始转写文本
~~~

AgenticASR 负责：

~~~text
原始转写
→ 去除口语词、重复、口吃
→ 处理自我修正
→ 恢复标点和断句
→ 修正术语、实体、数字格式
→ 防止模型误删、误改、幻觉
→ 输出 clean_text
~~~

主要说明见：

- [README.md](README.md)
- [当前精修规则与实现说明.md](当前精修规则与实现说明.md)

当前项目已经具备完整闭环，但仍属于研究型可运行原型，尚未达到配置完全统一、指标完整、经过真实长音频验收的生产系统阶段。

## 二、当前完整处理链

当前 Web/实时主流程是：

~~~text
Qwen/Whisper/sherpa ASR
        ↓
VAD、分段、窗口合并
        ↓
实体、URL、邮箱、标识符保护
        ↓
模糊实体匹配和候选标准化
        ↓
数字规则预处理
        ↓
off / conservative / tri_state 门控
        ↓
Refiner 模型
        ↓
结构化 patch 解析
        ↓
占位符恢复和完整性检查
        ↓
数字、内容、重复、语义、标点检查
        ↓
失败则重试
        ↓
仍失败则安全回退到 ASR 原文
        ↓
确定性清理
        ↓
clean_text + 完整审计日志
~~~

核心实现文件：

- system/web_app.py
- system/entity_pipeline.py
- system/refinement_gate.py
- system/refinement_guard.py
- system/window_refinement.py

## 三、分段和窗口规则

### 1. 普通分段

默认优先按照句末标点切分：

~~~text
。！？!?；
~~~

如果句子过长，则使用标点和最大字符数兜底。模型窗口默认最大约 80 个可见字符。

### 2. tri_state 流式分段

Web 的 tri_state 模式更激进地使用标点切块：

~~~text
，、；：。！？!?；
~~~

每个标点都可以结束一个窗口。当前最多保留最近 3 个标点块，并受 80 字符上限约束。

例如：

~~~text
我觉得，今天应该，先做测试。
~~~

可能被组织成最近三个连续语义块，而不是等待完整长句。

### 3. 小数点保护

以下小数点不会作为窗口边界：

~~~text
4.5
1.7
~~~

### 4. 自我修正窗口合并

识别以下自我修正标记：

~~~text
不对
我说错了
说错了
应该是
应该说
准确地说
我是说
改成
不是
不，是
而是
~~~

发现这些标记后，会回溯最多 3 个前置块，并临时扩大窗口，使模型能够看到：

~~~text
错误说法 + 修正标记 + 正确说法
~~~

最多允许两个短口癖块参与恢复，活动文本大约不超过 100 字。

### 5. 标点规则

- 模型不能随意删除窗口原有的结束标点。
- 模型省略标点时，会触发重试或回退。
- 未结束的片段不会自动补句号。
- 只有确定性删除口癖、重复时，才允许连带删除冗余标点。
- 已审核的错断短语可以跨窗口修复。

## 四、实体和术语保护规则

### 1. 保护对象

当前 Web/实时路径会保护：

- SQLite 实体库中的 canonical 名称
- 实体别名
- URL
- 邮箱
- 日期、时间、数字类规则对象
- 标识符
- 英文缩写
- 高可信历史实体
- 固定数字成语

但是当前 Web 明确设置了：

~~~python
protect_numeric_spans=False
~~~

因此普通数字、日期、时间、金额通常不会变成占位符。

占位符形式：

~~~text
__ENTITY_000__
__ENTITY_001__
~~~

### 2. 占位符完整性

模型输出后必须满足：

- 每个占位符仍然存在
- 每个只出现一次
- ID 合法
- 顺序不能改变
- 不能产生未知占位符
- 不能破坏下划线结构
- 不能把实体拆开
- 不能无依据新增实体

否则直接拒绝候选结果。

### 3. 实体库

SQLite 中保存：

- canonical
- entity_type
- domain
- policy
- priority
- enabled
- aliases
- source

支持：

- 增加实体
- 修改实体
- 删除实体
- 启用/禁用实体
- 按领域查询
- 维护别名

### 4. 模糊匹配

模糊匹配会对文本做：

- Unicode NFKC
- 大小写归一化
- 标点和空格忽略
- 中文字符相似度
- 拼音 bigram 相似度
- 长度差判断
- 领域权重
- 优先级权重

主要评分权重：

~~~text
拼音相似度：0.55
字符相似度：0.25
长度：0.10
领域：0.08
优先级：0.02
~~~

自动替换需要：

- 分数达到阈值
- 拼音分数足够高
- 与第二候选有足够 margin
- 实体类型属于低风险类型
- policy 允许 normalize

低风险自动替换主要包括：

~~~text
TERM
PROJECT
MODEL
~~~

PERSON、ORG 等高风险实体默认更加保守。

## 五、数字规范化规则

实现文件：system/numeric_normalizer.py

### 1. 支持的数字

支持：

~~~text
零 〇 一 二 两 三 四 五 六 七 八 九
~~~

支持中文位值：

~~~text
十 百 千 万 亿
~~~

支持小数：

~~~text
点
~~~

### 2. 百分比

~~~text
百分之三十七 → 37%
百分之五 → 5%
~~~

### 3. 日期

~~~text
二零一五年十二月五日 → 2015年12月5日
二零二四年六月 → 2024年6月
~~~

支持：

~~~text
年
月
日
号
~~~

### 4. 时间

~~~text
七点十分 → 7点10分
八点三十分钟 → 8点30分钟
~~~

### 5. 计量单位

支持的单位包括：

~~~text
平方公里、平方米、立方米
公里、千米、厘米、毫米
公斤、千克、毫升
小时、分钟、秒
人民币、块钱、元
平米、米、克、吨、升
度、岁
~~~

### 6. 数量单位

支持：

~~~text
个、只、件、本、张、台、条、位、名、辆
套、箱、袋、杯、份、枚、颗、粒、栋、间
户、艘、架、门、次
~~~

例如：

~~~text
五个苹果 → 5个苹果
两辆车 → 2辆车
三百人 → 300人
~~~

### 7. 倍数、折扣、成数

~~~text
三倍 → 3倍
八成 → 8成
七折 → 7折
~~~

### 8. 范围

~~~text
一到两点五米 → 1到2.5米
三至五公里 → 3至5公里
~~~

### 9. 大单位

当前代码保留“万、亿”单位：

~~~text
十亿 → 10亿
一点七亿 → 1.7亿
一百二十三万 → 123万
~~~

这里存在文档与代码不一致：

- 文档某处写成 一百二十三万 → 1230000
- 当前实际代码输出是 一百二十三万 → 123万

当前应以代码行为为准。

### 10. 概数

支持保留：

~~~text
三千多个 → 3000多个
五十多岁 → 50多岁
两百来人 → 200来人
~~~

### 11. 模糊数字保持原样

以下情况不会强行转换：

~~~text
二三个人
二三十个人
十几个苹果
二十多人
二三
一三八零……
~~~

出现以下后缀时通常保守处理：

~~~text
几
多
来
余
~~~

### 12. 固定数字成语

固定表达不会被转换，包括：

~~~text
一五一十
一心一意
三心二意
不三不四
乱七八糟
七上八下
五湖四海
四面八方
九牛一毛
十全十美
百里挑一
千方百计
万无一失
一举两得
三番五次
一清二楚
说一不二
十有八九
七七八八
三三两两
五花八门
五颜六色
千言万语
千军万马
千变万化
千山万水
万水千山
九死一生
八九不离十
四通八达
四平八稳
两全其美
两肋插刀
一针一线
一朝一夕
一刀两断
一穷二白
一来二去
一干二净
一百年大计
一百年好合
一千年一遇
百年大计
百年好合
千年一遇
~~~

另外还保护一些容易误判的词：

~~~text
万一
千万
亿万
万万
一度
一块
~~~

## 六、量词叠词和重复保护

项目专门防止模型误删量词叠词，例如：

~~~text
一朵朵
一层层
一本本
一艘艘
一群群
一排排
~~~

保护方式有两层。

### 1. 量词词表

覆盖：

- 名量词
- 容器量词
- 集合量词
- 形状量词
- 时间量词
- 度量量词
- 动量词

### 2. 结构规则

不只依赖词表，而是识别：

~~~text
限定词/数词 + AA
~~~

因此能够保护未知但结构上明显是量词叠词的形式。

模型如果删除一个叠词字符，例如：

~~~text
一朵朵 → 一朵
~~~

会被安全检查拒绝。

普通词语的合法叠词也会保护，例如：

~~~text
人人
天天
看看
慢慢
~~~

以下重复属于专门的量词结构：

~~~text
根根
条条
座座
道道
代代
~~~

## 七、确定性文本清理规则

实现文件：system/deterministic_cleanup.py

确定性清理位于模型之后，主要处理模型不稳定但规则明确的问题。

### 1. 口癖

主要处理：

~~~text
呃
额
嗯
~~~

但有边界限制：

~~~text
呃逆
嗯哼
~~~

不会误删。

### 2. 句首、句尾口癖

例如：

~~~text
他说。嗯，明天开始
~~~

可能清理为：

~~~text
他说。明天开始
~~~

### 3. 重复人称和指示词

识别：

~~~text
我我
你你
您您
他他
她她
它它
这这
那那
~~~

### 4. 逗号内部重复

例如：

~~~text
不，不，不
少主，少主
~~~

重复次数达到条件且短语足够短时，保留一次。

### 5. 句间重复

例如：

~~~text
我们开始了。我们开始了。
~~~

会识别连续重复短句，默认保留第一次。

限制：

- 句子内容较短
- 相邻句子相同
- 只在明确边界上处理
- 不进行全局删除

### 6. 已审核字幕错断修复

system/transcript_repairs.py 中保存的是精确短语映射，不是自由文本替换。

已覆盖的类型包括：

~~~text
滔滔。香水 → 滔滔江水
华夏。在文明 → 华夏文明
一旦决。口 → 一旦决口
最大径流量。更是 → 最大径流量，更是
通过善去改造。恶的本性 → 通过善去改造恶的本性
正义就是那种。最好的东西 → 正义就是那种最好的东西
一。让长江水直接凌空而过 → 一座让长江水直接凌空而过
过过度放牧 → 过度放牧
早已换了模样 → 已早已换了模样的重复修正
~~~

系统不会对所有句号做全局删除，避免把真正的句子边界误删。

## 八、Refiner 模型规则

协议文件：system/refinement_protocol.py

当前协议版本：

~~~text
v3-model-numeric-idiom-guard
v3-structured-patch
~~~

模型必须遵守：

- 最小修改
- 不总结
- 不扩写
- 不添加原文没有的事实
- 不改变人物、数量、否定关系和动作
- 去除口癖和明显重复
- 处理自我修正
- 改善标点
- 保持实体和占位符
- 不擅自改变数值

当前优先使用结构化 patch：

~~~json
{
  "action": "replace",
  "source": "原文中唯一连续片段",
  "target": "替换后的片段",
  "reason": "repetition"
}
~~~

允许的 reason 包括：

~~~text
no_change
disfluency
repetition
self_correction
boundary_punctuation
local_typo
~~~

source 必须：

- 是输入中的真实连续片段
- 只出现一次
- 不能是模型凭空编造的片段

如果 JSON 解析失败，仍兼容旧的纯文本输出，但会进入更严格的安全检查。

## 九、三态门控规则

实现文件：system/refinement_gate.py

### 1. off

非空片段直接尝试调用 Refiner。

### 2. conservative

以下情况倾向调用 Refiner：

- 有口癖
- 有重复
- 有自我修正
- 有数字格式问题
- 有实体提示
- 置信度缺失
- 置信度未校准
- 文本不稳定
- 句子未完成
- 文本覆盖不完整

干净且高置信度的完整片段可以跳过。

### 3. tri_state

输出三种状态：

~~~text
KEEP
DEFER
REFINE
~~~

#### KEEP

通常满足：

- 当前文本稳定
- 没有明显清理信号
- 没有实体或数字问题
- 句子完整
- 没有明显修订
- 置信度足够，或属于安全保留场景

#### DEFER

通常表示：

- 文本仍在增长
- 尾部太新
- ASR 假设仍不稳定
- 当前修订比例太高
- 不适合立即调用模型

#### REFINE

以下情况会触发：

- 口癖
- 重复
- 自我修正
- 数字格式问题
- 标点异常
- 实体候选
- 跨块断句异常
- 句子完整但明显存在清理需求
- 最终事件需要完成处理

当前 Web 默认启动模式是 tri_state。

## 十、安全校验和回退

实现文件：system/refinement_guard.py

模型输出可能因为以下原因被拒绝：

~~~text
empty
repeated_sentence
repeated_phrase
severe_content_loss
short_semantic_loss
truncated
numeric_value_mismatch
semantic_content_loss
semantic_substitution
unsupported_insertion
negation_change
protected_repetition_deletion
source_punctuation_lost
placeholder_missing
placeholder_duplicate
placeholder_order_changed
unknown_placeholders
malformed_placeholder
entity_boundary
~~~

主要阈值：

- 一般窗口最大约 80 字符
- 严重内容损失比例约 0.65
- 短文本相似度低于约 0.70 会拒绝
- 长文本超过 100 字符且输出缩短到原文 80% 以下，并且没有正常结束标点，会视为截断
- 数字比较不仅比较字符串，还比较数值、单位和顺序

### 安全挽救

如果模型整体候选被拒绝，但其中存在安全修改，会尝试保留：

- 等价数字表面变化
- 已证明的相邻重复删除

其他模型修改不会进入最终结果。

最终仍失败时，回退到：

~~~text
规则处理后的原始 ASR 文本
~~~

## 十一、ASR 和服务结构

### 1. Qwen 流式 ASR

实现文件：system/qwen_asr_stream_server.py

默认：

~~~text
Qwen ASR 端口：8766
Web 端口：8081
采样率：16 kHz
VAD：Silero
VAD 阈值：0.5
最小静音：约 0.7 秒
最小语音：约 0.25 秒
预录音：约 0.7 秒
尾部补偿：约 1 秒
~~~

支持：

~~~text
/stream/start
/stream/chunk
/stream/finish
/stream/cancel
~~~

也兼容旧的 legacy_v1 协议。

### 2. Whisper 流式路径

Whisper 路径采用累计音频反复识别：

~~~text
已有音频 + 新音频 → 重新转写
~~~

它不像 Qwen 路径那样提供完整的 VAD 完成段和置信度，因此不同后端的行为还没有完全统一。

### 3. Web 前端

前端支持：

- 在线麦克风
- 离线完整音频
- 文件流式识别
- 普通、医疗、法律、教育、金融、技术领域
- 实体库管理
- 显示 raw_text
- 显示 clean_text
- 显示 KEEP/DEFER/REFINE
- 显示精修历史
- 显示拒绝原因和回退原因

## 十二、批处理、实验和数据管线

### 1. 批处理

experiments/postprocess_asr.py 可以批量处理 ASR JSONL。

但它与 Web 路径并不完全一致：

- 默认门控配置不同
- 数字处理不同
- 实体保护方式不同
- 没有完全复用 Web 的窗口逻辑
- 没有完全复用安全挽救逻辑
- 使用旧版调用协议较多

这是当前项目最重要的工程一致性问题之一。

### 2. 数据生成管线

pipeline/ 设计目标是生成约 100000 条训练数据。

流程：

~~~text
场景种子
→ 口语文本
→ 清洗文本
→ 模拟 ASR 错误
→ 数据组装
→ 质量检查
→ 去重
→ train.jsonl
→ SFT
~~~

包含 16 类场景，例如：

- 日常聊天
- 英语口语
- Vibe Coding
- 解释说明
- 会议
- 客服
- 学术
- 导航
- 听写
- 语音搜索

但当前仓库没有完整生成好的训练数据闭环，仍依赖外部 LLM/vLLM 和机器相关配置。

### 3. 评测工具

已有：

- CER
- WER
- MER
- 平均延迟
- AASR-Bench Judge
- 置信度温度校准
- 结果存储和幂等处理

但是还没有完成完整的：

- 固定测试集
- 双参考文本
- 跨 ASR 后端对照
- 门控消融实验
- 误替换率
- 回退率
- 调用次数
- p50/p95 延迟报告

## 十三、当前测试和代码状态

实际执行结果：

~~~text
pytest -q
268 passed, 1 warning
~~~

另外：

~~~text
python -m compileall -q system experiments pipeline
~~~

通过。

~~~text
git diff --check
~~~

通过。

但是文档中的测试数字已经过时：

- README 写 215 passed
- 其他文档写 212、188 等
- 当前真实结果是 268 passed

唯一测试警告是 Starlette/httpx TestClient 的弃用提醒。

这些测试主要覆盖：

- 数字转换
- 占位符
- 实体保护
- 门控
- 安全校验
- Web finish
- 窗口合并
- 确定性清理

它们并不等于：

- GPU 模型真实推理通过
- 浏览器真实麦克风验收通过
- 长音频端到端稳定
- Qwen/Refiner 当前服务正在运行
- 真实延迟满足要求

## 十四、当前工作区状态

当前 Git 分支是：

~~~text
main
~~~

工作区存在大量未提交变更：

- 18 个已修改文件
- 多个新增结果 JSON
- 新增 PPT 和汇报材料
- 新增离线测试脚本
- 新增服务启动脚本
- 新增测试音频
- results/web/session.jsonl 也被修改

主要近期修改集中在：

- system/refinement_guard.py
- system/web_app.py
- system/deterministic_cleanup.py
- system/numeric_normalizer.py
- system/window_refinement.py

当前实现显然处于一轮较大的规则增强和线上测试阶段，还没有整理成稳定发布版本。

## 十五、目前发现的关键不一致

### 1. 数字规范化开关不一致

文档描述倾向于“数字规范化已开启”。

但是当前：

~~~text
system/web_app.py 默认 numeric_normalization=False
scripts/start_web.sh 没有传 --enable-numeric-normalization
~~~

所以直接执行 start_web.sh 时，前置数字规范化并不会完整开启。

更复杂的是，模型被拒绝或 Refiner 忙时，代码仍会在部分回退路径中调用数字规范化。因此当前的“关闭”并不是完全关闭，属于行为不一致。

### 2. 实体数据库默认路径不一致

start_web.sh 默认寻找：

~~~text
/home/aim0/桌面/tan/ASR/Codex-web/data/entities.db
~~~

这个文件当前不存在。

start_services.sh 默认寻找：

~~~text
/home/aim0/桌面/tan/ASR/AgenticASR_entity_e5bedb3/data/entities.db
~~~

这个文件当前存在。

因此：

- 直接运行 start_web.sh 可能启动失败
- 运行 start_services.sh 更接近当前机器的真实配置
- 最好统一实体库路径

### 3. 文档测试数字过时

代码已经达到 268 个测试通过，但文档仍保留旧数字，容易造成误判。

### 4. Web、批处理、实时客户端没有完全统一

三条路径在以下方面仍有差异：

- 数字处理
- 门控默认值
- 实体保护
- Refiner 协议
- 窗口策略
- 回退逻辑
- 结果字段

目前 Web 路径是实现最完整的一条。

### 5. 置信度尚未完成校准

Qwen 可以提取 token logprob，但当前置信度通常：

- 未校准
- 只覆盖生成后缀
- 不能直接当成真实概率

所以 tri_state 当前采取了较保守的策略：没有可靠置信度时，不因为一个高分就盲目跳过所有安全检查。

## 十六、尚未完成的核心工作

根据代码和文档，当前还没有完成：

1. 统一的 ASRHypothesis 数据协议。
2. 稳定前缀和动态 active span。
3. Web、批处理、实时客户端的统一处理语义。
4. 完整结构化 Refiner schema 和 Validator。
5. 双参考数据集。
6. 可复现的 replay 流式实验。
7. 跨 ASR 后端统一评测。
8. 置信度阈值的独立验证。
9. 真实长音频和浏览器端延迟验收。
10. 误替换率、回退率、调用次数、p50/p95 延迟的正式报告。
11. 训练协议与当前运行时协议的完全统一。
12. 生产环境下的实体库、模型路径和启动配置整理。

## 十七、最终项目判断

当前项目不是半成品代码，而是一个已经完成主要工程闭环的研究型原型。

目前最成熟的部分是：

- 实体保护
- 数字安全校验
- Refiner 拒绝和回退
- 三态门控
- 流式窗口合并
- 重复和口癖清理
- 结构化 patch
- Web 调试和审计日志
- 自动化测试

目前最需要继续收敛的部分是：

- 统一各入口行为
- 修正启动配置
- 统一文档与代码
- 完成真实音频评测
- 完成门控和置信度实验
- 建立稳定版本和可复现实验结果

一句话总结：

> AgenticASR 当前已经是“可运行、可测试、具备安全回退机制的 ASR 精修原型”，核心规则体系相当丰富；但目前仍处于规则快速迭代和实验验证阶段，还不能称为配置完全统一、指标完整、经过真实长音频验收的生产系统。

