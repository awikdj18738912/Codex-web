# AgenticASR 两页 PPT 汇报稿

## 第 1 页：相对于原始 AgenticASR 的系统改进

原始 AgenticASR 的核心链路是 VAD、在线 ASR、ChunkManager 和 K=3 滑动窗口 Refiner，重点解决口语清理、自我修正和书面化表达。

当前项目在这个基础上做了三类扩展。第一类是实体和术语保护：增加 SQLite 实体库，支持标准名称、别名、领域管理以及受控匹配；精修前进行占位保护，精修后恢复并校验，降低人名、地名和作品名被模型改写的风险。第二类是服务架构扩展：增加 Qwen3-ASR GPU 服务、Transformer Refiner 和 Web 前端，支持文件、实时和离线流式转录。第三类是精修控制和安全校验：增加 tri_state 门控、数字和量词保护、标点与内容完整性检查、结构化 JSON 局部修改，以及失败回退和日志审计。

所以，当前版本的核心变化不是单纯更换模型，而是把原版依赖模型完成的精修过程，扩展成了“模型精修、实体保护、多层校验和结果回退”的完整运行时管线。

## 第 2 页：改造后的问题与原因

这些改造提高了系统的可控性，但也增加了模块之间的耦合。当前主要问题是：叠字修复不够稳定，正常叠词与口吃叠字难以区分，部分重复仍会漏修；VAD、ASR 分段、窗口聚合和标点恢复之间存在耦合，句号和逗号仍可能出现错位，导致句子拆开或粘连；实体、数字和成语保护有时会阻止模型进行本来正确的修改；额外的局部复核也增加了 GPU 调用和处理延迟。

这些问题主要来自五个方面：流式窗口提供的上下文有限；VAD、ASR 和精修模块相互影响；同一重复形式存在多种语义；安全校验偏保守会拒绝部分高风险修改；模型偶尔不按结构化协议返回，导致重试或回退。

当前结论是：系统的安全性和可控性提升了，但精修准确率仍受到上下文、规则和模型稳定性的共同影响。
