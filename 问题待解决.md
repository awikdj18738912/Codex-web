# 待解决问题跟踪

## 已处理：量词叠词被误去重

- `一根根`、`一条条`、`一座座`、`一道道`、`一代代` 等重叠量词已加入保护。
- 已扩展广覆盖量词词表，并增加“限定词 + 任意汉字 AA”的结构兜底，覆盖 `一朵朵`、`一层层`、`一本本`、`一艘艘`、`一群群` 等新形式。
- `源源不断`、`生生不息` 等固定叠词已加入保护；Refiner 删除一个重叠字会被语义校验拒绝并回退。

## 仍需真实音频验收：精修延迟

- 当前 Web 的 `tri_state` 会按一个标点切块，并以最近三个块组成活动窗口；模型调用仍可能产生可感知延迟，需要用真实录音继续测量端到端耗时。

## 已处理：最新样本的字幕错断与固定搭配错误

- 已新增 `system/transcript_repairs.py`，精确修复 `华夏。在文明`、`滔滔。香水`、`最大径流量。更是`、`一旦决。口` 等已审核短语。
- 已修复 `过过度放牧`、`遍布全舰`、`已早已`，并整理“错峰进入下游”重复且矛盾的句子。
- `黄河园区`与“黄河源区/黄河源园区”存在语义歧义，暂不做无条件替换，交由术语库或结构化补丁按上下文确认。
- 对 `滔滔江水`、`一座渡槽` 等补字只在完整短语匹配时执行；普通未知句号不会被全局删除。
