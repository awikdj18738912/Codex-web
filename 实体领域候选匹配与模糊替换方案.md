# AgenticASR 实体候选匹配与安全替换实施方案

> **状态说明（2026-09-16）**：以下实施方案部分已有代码对应；当前规则和实际入口状态以 system/entity_pipeline.py、system/entity_matcher.py 及 [当前项目状态](CURRENT_WORK_AND_NEXT_STEPS.md) 为准。本文件保留候选评分和评测设计，不代表阈值已通过正式数据集验收。

## 1. 目标和边界

本方案直接面向当前 AgenticASR 仓库，将已有的“标准实体/别名精确匹配”升级为可验证的转录后实体纠错链路：

```text
ASR 转录
  → 精确别名匹配
  → 字符/拼音候选召回
  → 分数、候选间隔和风险判断
  → 结构化 span 替换决策
  → 占位符保护
  → Refiner 最小编辑
  → 恢复、校验和失败回退
```

第一版要处理：

- 中文同音、近音、单字错误和轻微增删，例如“鬼灵们 → 鬼灵门”。
- 英文/中英混合实体的空格、连字符、大小写和轻微编辑错误，例如 `Agentic SR → AgenticASR`。
- 只从用户已维护的 SQLite 实体库选择标准值。
- 自动替换、只提示和保留原文三种决策全部可审计。

第一版不做：

- 不将 Refiner 推测的实体自动写回永久术语库。
- 不让 LLM 自由生成词库之外的替换值。
- 不引入向量库、知识图谱、GNN 或多轮 Agent。
- 中间流式窗口也允许通过阈值的低风险模糊实体自动替换；若 ASR 后续改写
  source span，必须按原文归属使缓存失效并重新匹配。
- 不使用当前 ASR 未必提供的 confidence 决定自动替换。

## 2. 当前代码现状

### 2.1 可直接复用的模块

| 文件 | 已有能力 | 复用方式 |
|---|---|---|
| `system/entity_store.py` | SQLite 实体、别名、领域、类型、规范化策略、优先级 | 作为唯一允许自动写入转录的实体来源 |
| `system/protection.py` | 精确实体/别名匹配，数字、日期、URL 等占位保护及恢复校验 | 接收通过决策的模糊实体 span |
| `system/session_memory.py` | 会话级实体信任与 TTL | 继续保护已验证实体，不自动学习错误别名 |
| `system/web_app.py` | 领域选择、三种转录模式、Refiner、JSONL | 主集成入口 |
| `system/live_qwen_refiner.py` | Qwen3-ASR + Refiner 麦克风链路 | 接入同一实体管线 |
| `experiments/scripts/postprocess_asr.py` | 离线批处理 | 用于影子模式、阈值调优和 A/B 评测 |

### 2.2 当前集成状态与剩余工作

本节替代早期版本中“三个入口都未将掩码文本送入 Refiner”的历史判断。当前 Web 主路径已接入实体准备/掩码、Refiner 调用、恢复、内容/数字检查、必要时严格重试及失败回退；统一流程的核心实现位于 system/entity_pipeline.py，并由 system/web_app.py 使用。

模糊匹配也已经有 system/entity_matcher.py 与配置文件 system/configs/entity_matching.json；支持 off、shadow、hint、auto 模式。auto 并非任意字符串替换：目前限于严格阈值、低风险类型和适用的最终/完整文本，PERSON/ORG 不做 fuzzy auto。

仍需完成的工作是跨 Web、批处理和本地客户端统一审计 schema、接受标志和 reject reason，并通过集成测试确认每个入口都对保护失败执行相同回退。候选阈值、领域迁移能力和实体误改率也仍须在独立标注集上验证。本方案后续章节保留为设计依据，不应当被视为所有步骤均已交付。

## 3. 目标文件与单段处理链路

### 3.1 新增文件

```text
system/entity_matcher.py
system/configs/entity_matching.json
experiments/scripts/evaluate_entity_correction.py
tests/fixtures/entity_correction_cases.jsonl
tests/test_entity_matcher.py
tests/test_entity_pipeline.py
```

### 3.2 需修改文件

```text
system/protection.py
system/web_app.py
system/live_qwen_refiner.py
system/window_refinement.py
experiments/scripts/postprocess_asr.py
experiments/scripts/postprocess_contract.py
experiments/scripts/metrics.py
system/web/index.html
requirements.txt
system/requirements.txt
system/README.md
```

### 3.3 唯一处理顺序

```python
report = matcher.match(segment, allow_auto=True)
protection = protector.protect(segment, fuzzy_matches=report.auto_matches)

# Refiner 失败时的可靠基线：只含已通过规则的替换。
baseline = protector.restore(protection.masked_text, protection)

hints = merge_hints(
    protector.refinement_hints(protection),
    report.hint_canonicals,
    limit=16,
)
refined_masked = refiner.refine(protection.masked_text, entity_hints=hints)
restored = protector.restore(refined_masked, protection)

if not restored.accepted:
    clean_segment = baseline.text
elif reject_reasons(baseline.text, restored.text):
    clean_segment = baseline.text
else:
    clean_segment = restored.text
```

这保证 Refiner 超时、截断或损坏占位符时，已通过高置信规则的实体纠错仍然保留，但不保留不可验证的 LLM 改写。

## 4. `system/entity_matcher.py` 接口

```python
class EntityDecision(str, Enum):
    EXACT = "EXACT"
    AUTO_NORMALIZE = "AUTO_NORMALIZE"
    HINT_ONLY = "HINT_ONLY"
    KEEP_RAW = "KEEP_RAW"


@dataclass(frozen=True, slots=True)
class EntityMatch:
    start: int
    end: int
    observed: str
    entity_id: int
    canonical: str
    entity_type: str
    domain: str
    matched_surface: str
    match_type: str
    char_score: float
    pinyin_score: float
    length_score: float
    domain_score: float
    priority_score: float
    final_score: float
    runner_up_score: float | None
    margin: float | None
    decision: EntityDecision
    reasons: tuple[str, ...]


@dataclass(frozen=True, slots=True)
class EntityMatchReport:
    matches: tuple[EntityMatch, ...]
    matcher_latency_ms: float

    @property
    def auto_matches(self) -> tuple[EntityMatch, ...]: ...

    @property
    def hint_canonicals(self) -> tuple[str, ...]: ...


class EntityCandidateMatcher:
    def __init__(
        self,
        entities: tuple[EntityDefinition, ...],
        *,
        selected_domain: str,
        config: EntityMatchConfig,
    ) -> None: ...

    def match(self, text: str, *, allow_auto: bool) -> EntityMatchReport: ...
```

第一版增加两个轻量依赖：

```text
pypinyin>=0.53,<1
rapidfuzz>=3.10,<4
```

## 5. 候选召回

### 5.1 归一化

归一化只用于召回和评分，不直接写回转录：

1. Unicode NFKC。
2. ASCII casefold。
3. 评分时忽略空格、连字符、下划线和普通标点。
4. 保留归一化字符到原文 offset 的映射。
5. 拼音使用 `Style.NORMAL`；ASCII 块保留 casefold 形式。

替换必须使用原文 `start/end` offset，禁止对整句使用全局 `str.replace()`。

### 5.2 召回限制

每个实体的 canonical 和 aliases 预计算归一化字符、拼音音节、长度、字符 bigram 和拼音 bigram。对每个最多 200 字符的 segment：

1. URL、邮箱、日期、时间、数字和标识符 span 不进入模糊召回。
2. 候选不跨越 `。！？!?\n`。
3. 候选与实体长度相差不超过 `max(1, min(2, floor(entity_len * 0.25)))`。
4. 必须共享至少一个字符 bigram 或拼音 bigram，才进入详细评分。
5. 单字中文实体和归一化后少于 4 个字符的纯 ASCII 实体禁止模糊自动替换。
6. 一个 span 最多保留 top-3，一个 segment 最多记录 20 个候选。

实现采用“长度桶 + bigram 倒排索引”，不做全文子串与全实体库的两两比较。

## 6. 评分和决策

### 6.1 评分公式

`EntityStore.list_entities(domain=selected_domain)` 已将候选限定为“当前领域 + general”。第一版不遍历其他领域，也不做自动领域分类。

```text
final_score =
    0.55 * pinyin_score
  + 0.25 * char_score
  + 0.10 * length_score
  + 0.08 * domain_score
  + 0.02 * priority_score
```

- `pinyin_score`：拼音音节序列的归一化 Levenshtein 相似度。
- `char_score`：归一化字符序列的归一化 Levenshtein 相似度。
- `length_score = 1 - abs(span_len - entity_len) / max(span_len, entity_len)`。
- `domain_score`：当前领域为 1.0，`general` 为 0.8。
- `priority_score`：将数据库 `priority` 夹到 `[0, 20]` 后除以 20。

### 6.2 自动替换

以下条件必须全部成立：

```text
normalization_policy == "normalize"
entity_type in {"TERM", "PROJECT", "MODEL"}
final_score >= 0.80
pinyin_score >= 0.88
best_score - runner_up_score >= 0.12
event 是 final 或离线完整转录
span 不与现有规则保护区重叠
```

`PERSON` 和 `ORG` 第一版一律禁止模糊自动替换；只能精确别名规范化或降级为 `HINT_ONLY`。后续新增的医疗、金融和法律类型默认也是高风险。

### 6.3 `HINT_ONLY`

```text
final_score >= 0.80
pinyin_score >= 0.75
```

或者分数达标但存在任一降级原因：

- 候选间隔不足。
- 实体类型高风险。
- `normalization_policy == "preserve"`。
- 当前是流式 partial/update。
- 候选太短。

Refiner 一次最多接收 16 个 hint，按决策级别、分数和实体优先级降序排列并去重。

### 6.4 `KEEP_RAW`

得分低于 0.80、只有一个弱信号、与更高优先级 span 冲突，或不在当前领域库中时，保留原文。

## 7. span 冲突和安全替换

每次替换前必须重新验证：

```text
text[start:end] == observed
replacement == EntityDefinition.canonical_text
entity_id 仍然存在且 enabled == true
entity.domain in {selected_domain, "general"}
```

冲突排序固定为：

```text
EXACT > AUTO_NORMALIZE > HINT_ONLY > KEEP_RAW
→ final_score 更高
→ span 更长
→ priority 更高
→ entity_id 更小（稳定 tie-breaker）
```

选中的模糊 span 直接转换为 `ProtectedSpan`，其 `replacement` 是 canonical。这样不需要先改文本再重新查找，也避免 offset 漂移。

## 8. 对现有模块的具体修改

### 8.1 `system/protection.py`

- `ProtectedSpan` 增加 `entity_id`、`match_type`、`match_score`、`decision`。
- `protect()` 增加 `fuzzy_matches` 参数，只接收 `AUTO_NORMALIZE` span。
- 模糊实体 `source` 记为 `fuzzy:<domain>`，精确别名优先级始终更高。
- `public_dict()` 输出新的审计字段。
- 保持现有“未知、缺失、重复、乱序占位符必须拒绝”的行为。

### 8.2 `system/web_app.py`

- 每个 WebSocket 会话创建一个 matcher，只加载请求领域 + `general`。
- `refine_update()` 真正使用 `masked_text` 和 `restore()`。
- `fallback_update()` 执行确定性实体 baseline，不调用 Refiner。
- 中间 `update` 和最终 `final` 都设置 `allow_auto=True`；ASR 改写时依赖 source-owned cache invalidation 重新匹配，而不是延迟到结束才规范化。
- 响应和 JSONL 增加 `entity_candidates`、`entity_matcher_latency_ms`、`entity_fuzzy_mode`。
- `entity_normalizations` 只记录实际应用的替换。

### 8.3 `system/live_qwen_refiner.py`

- 增加 `--entity-domain`，默认 `general`，避免当前加载全部领域实体。
- 增加 `--entity-fuzzy-mode off|shadow|hint|auto`。
- 复用 Web 路径的共享处理函数，不复制评分和回退代码。
- 控制台和 JSONL 输出已应用替换及分数。

### 8.4 `experiments/scripts/postprocess_asr.py`

- 增加 `--entity-domain` 和 `--entity-fuzzy-mode`。
- 批处理输入改为 `masked_text`，生成后逐条 `restore()`。
- 执行与在线链路相同的 `reject_reasons()` 和 baseline 回退。
- 将候选、已应用替换和 matcher 延迟写入 `InferenceOutcome` 及 JSONL。

### 8.5 `system/window_refinement.py`

在 committed/active 结果合并中增加 `entity_candidates` 展平和 `entity_matcher_latency_ms` 求和，不丢失已提交 chunk 的实体决策。

### 8.6 `system/web/index.html`

在精修历史卡片中增加默认折叠的“实体候选”：

```text
鬼灵们 → 鬼灵门
拼音 1.00 / 字符 0.67 / 总分 0.93 / 间隔 0.18
决策：已自动规范化
```

前端只展示服务端结果，不重新计算分数。

## 9. 运行模式

```text
off     仅保留现有精确别名逻辑
shadow  计算候选并写日志，不提示、不替换
hint    候选可进入 Refiner hint，不做确定性替换
auto    每个窗口都允许低风险高置信候选自动替换，final 再做幂等复核
```

代码首次合并后默认仍建议使用 `shadow` 做评测。生产配置为 `auto` 时，中间
`update` 和 `final` 都可改变文本，但只限于评分、间隔、实体类型和
`normalize` 策略全部通过的候选。

## 10. 配置

`system/configs/entity_matching.json`：

```json
{
  "version": 1,
  "weights": {
    "pinyin": 0.55,
    "character": 0.25,
    "length": 0.10,
    "domain": 0.08,
    "priority": 0.02
  },
  "thresholds": {
    "hint_score": 0.80,
    "auto_score": 0.80,
    "auto_min_pinyin": 0.88,
    "auto_min_margin": 0.12
  },
  "limits": {
    "max_entities": 5000,
    "max_candidates_per_span": 3,
    "max_logged_candidates_per_segment": 20,
    "max_refiner_hints": 16
  },
  "auto_entity_types": ["TERM", "PROJECT", "MODEL"],
  "high_risk_entity_types": ["PERSON", "ORG"]
}
```

加载时验证权重之和为 1，阈值在 `[0,1]`，`hint_score <= auto_score`，限额为正整数。验证失败时拒绝启动，不静默使用错误配置。

## 11. 评测和硬性验收标准

### 11.1 测试数据

`tests/fixtures/entity_correction_cases.jsonl` 格式：

```json
{
  "id": "game-001",
  "domain": "game",
  "raw_text": "鬼灵们是这个副本的入口",
  "reference_text": "鬼灵门是这个副本的入口",
  "expected_matches": [
    {"observed": "鬼灵们", "canonical": "鬼灵门", "decision": "AUTO_NORMALIZE"}
  ],
  "negative": false
}
```

首次验收至少需要：

- 200 条真实或人工审核正样本。
- 200 条近音普通词、错领域、短实体和候选冲突负样本。
- 至少 30% 样本来自当前 Qwen3-ASR/Whisper 真实输出，不能全由规则合成。
- development/test 按 canonical 实体去重分割，避免同一实体泄漏到两边。

### 11.2 输出指标

```text
candidate_recall          正确实体是否进入 top-3
auto_precision            自动替换中正确的比例
auto_recall               标注实体中被正确自动替换的比例
negative_change_rate      负样本被自动修改的比例
NE-CER / NE-FNR           实体错误率和未命中率
overall CER / MER         整体错误率
placeholder fallback      占位符异常的安全回退率
matcher p50/p95 latency   候选匹配延迟
```

### 11.3 A/B 组

```text
A. raw ASR
B. 当前 exact alias only
C. fuzzy shadow
D. fuzzy hint only + Refiner
E. fuzzy auto（不启用 Refiner）
F. fuzzy auto + protected Refiner + validator
```

先用 E 验证 matcher 本身，再用 F 判断 Refiner 是否带来额外收益。

### 11.4 `shadow` 切换到 `auto` 的全部条件

```text
candidate_recall >= 90%
auto_precision >= 98.5%
PERSON/ORG 模糊自动替换数 == 0
negative_change_rate <= 0.5%
NE-CER 相对 exact-only 降低 >= 20%
overall CER 相对 exact-only 恶化 <= 0.1 个百分点
占位符故障注入的安全回退率 == 100%
5,000 实体、200 字符 segment 上 matcher p95 <= 20 ms
相同输入、词库和配置的决策可重现率 == 100%
```

未达标时保持 `shadow` 或 `hint`，优先提高间隔阈值、收紧可自动类型或补充负样本，不为了上线而降低精度要求。

## 12. 必须覆盖的测试

### 12.1 Matcher

- `鬼灵们 → 鬼灵门`、`Qwen ASR → Qwen3-ASR`、`Agentic SR → AgenticASR`。
- 全角/半角、大小写、空格和连字符。
- 同音的两个实体分数接近时必须 `HINT_ONLY`。
- 错领域实体不得进入候选。
- `preserve`、`PERSON`、`ORG`、单字中文和短 ASCII 实体不得模糊自动替换。
- 候选不跨标点，不覆盖 URL、邮箱、数字和日期。

### 12.2 Protection/Validator

- 通过的模糊 span 转换为占位符并恢复为 canonical。
- Refiner 删除、重复、新增或重排占位符时使用 baseline。
- offset 对应原文不一致时拒绝替换。
- 重叠实体只应用一个确定决策。
- Refiner 超时或截断时，高置信实体替换仍保留。

### 12.3 入口一致性

对同一文本、实体库、领域和配置，`web_app.py`、`live_qwen_refiner.py` 和 `postprocess_asr.py` 的候选、自动替换和拒绝原因必须一致。实体处理必须抽成共享函数，不在三个入口复制实现。

## 13. 实施顺序

### 阶段 0：基线

- 固定一份实体 SQLite fixture 和首批正/负 JSONL。
- 记录 raw 和 exact-only 的 CER/MER、NE-CER、NE-FNR。
- 验收：重复运行生成完全相同的报告。

### 阶段 1：闭合现有保护链路

- 三个入口全部使用 `masked_text → restore()`。
- 建立确定性 baseline 回退。
- 批处理补齐 exact alias 规范化。
- 验收：占位符故障注入 100% 回退，三条路径一致。

### 阶段 2：Matcher `shadow`

- 实现 matcher、配置校验、候选日志和单元测试。
- 输出 top-3、runner-up、margin 和降级原因。
- 验收：`candidate_recall >= 90%`，p95 达标，对用户输出零影响。

### 阶段 3：`hint`

- 中等置信候选进入现有 `<KEY>` 提示。
- 高风险实体仅提示。
- 验收：overall CER 恶化不超过 0.1 个百分点。

### 阶段 4：final/offline `auto`

- 只对 `TERM/PROJECT/MODEL + normalize` 应用高置信替换。
- Web 显示已应用和未应用候选。
- partial/update 仍为 hint-only。
- 验收：第 11.4 节条件全部达标。

### 阶段 5：可选优化

只在第一版实测显示明确瓶颈后考虑：

- 利用 N-best/多温度 ASR 假设恢复 1-best 中完全缺失的实体。
- 对多候选冲突使用小模型或 LLM 受限重排，输出必须是候选集内 `entity_id`。
- 连续两个稳定 ASR 假设一致后，再开放 partial 实体替换。

## 14. 实施后命令

离线影子评测：

```bash
python experiments/scripts/postprocess_asr.py \
  results/asr.jsonl \
  results/refined-shadow.jsonl \
  --model /path/to/AgenticASR-Refiner \
  --entity-db data/entities.db \
  --entity-domain game \
  --entity-fuzzy-mode shadow

python experiments/scripts/evaluate_entity_correction.py \
  --input results/refined-shadow.jsonl \
  --cases tests/fixtures/entity_correction_cases.jsonl \
  --output results/entity-shadow-report.json
```

验收通过后：

```bash
python -m system.web_app \
  --refiner-model /path/to/AgenticASR-Refiner \
  --refiner-device cuda:1 \
  --asr-url http://127.0.0.1:8766 \
  --entity-db data/entities.db \
  --entity-fuzzy-mode auto \
  --output results/web/session.jsonl \
  --port 8081
```

## 15. 日志契约

候选记录示例：

```json
{
  "start": 0,
  "end": 3,
  "observed": "鬼灵们",
  "entity_id": 17,
  "canonical": "鬼灵门",
  "entity_type": "TERM",
  "domain": "game",
  "match_type": "PINYIN_FUZZY",
  "char_score": 0.67,
  "pinyin_score": 1.0,
  "final_score": 0.897,
  "runner_up_score": 0.74,
  "margin": 0.186,
  "decision": "AUTO_NORMALIZE",
  "reasons": ["score_pass", "margin_pass", "final_event"]
}
```

运行级日志必须包含：

```text
entity_matching_config_version
entity_fuzzy_mode
selected_domain
entity_matcher_latency_ms
entity_candidates
entity_normalizations
protected_entities
refiner_accepted
refiner_reject_reasons
```

## 16. 与近期研究的对应

- [Generative Annotation for ASR Named Entity Correction](https://aclanthology.org/2025.emnlp-main.1052/)：候选召回、错误 span 定位和替换/拒绝。本项目第一版用可解释阈值代替训练生成式标注器。
- [RECOVER](https://arxiv.org/abs/2603.16411)：替换值限制在实体库，只输出受限 find/replace，并使用确定性 guardrail。
- [G-SPIN](https://arxiv.org/abs/2606.24889)：先用音素结构缩小搜索空间，再重排。第一版以拼音 bigram 倒排索引作为轻量替代。
- [CEGER](https://arxiv.org/abs/2509.14263)：使用结构化编辑而不是整段自由重写。本方案使用带 offset 的 `EntityMatch` 和 `ProtectedSpan`。

## 17. 完成定义

只有以下条件全部满足，才认为实体模糊替换第一版完成：

1. 三条主链路都执行掩码、恢复、校验和回退。
2. 不配置“鬼灵们”别名也能召回“鬼灵门”。
3. 只有当前领域 + `general` 的已验证实体可成为候选。
4. 自动替换可发生在中间和 final/offline 窗口，只针对低风险 `normalize` 实体；
   ASR 改写时必须重新匹配。
5. 中等置信和冲突候选不会被强制替换。
6. 任意占位符异常都回退到确定性实体 baseline。
7. SQLite 实体库不会被 ASR 或 Refiner 自动污染。
8. 日志可解释每次候选为什么被替换、降级或拒绝。
9. 第 11.4 节的精度、收益、负样本、整体 CER、安全回退和延迟指标全部达标。

核心原则：

> 先从已验证实体库缩小候选空间，再用字符、拼音、领域、候选间隔和风险策略决定是否替换；LLM 可以辅助精修，但不能绕过确定性验证直接改写实体。
