# AgenticASR 精修模型与规则精修注意事项

> **文档更新提示：** 本文保留了部分历史设计与启动参数，其中“数字占位符”、“量词不转换”和“去重阈值”等内容可能已过时。当前生效规则统一见 [当前精修规则与实现说明](当前精修规则与实现说明.md)。
> **更新提示（2026-09-16）**：本文包含早期参数示例和历史说明。运行行为、开关名称及当前模型路径以 scripts/start_qwen_asr.sh、scripts/start_web.sh 和 [当前精修规则与实现说明](当前精修规则与实现说明.md) 为准；不要直接复制没有重新核对过的旧启动参数。

本文说明当前项目在什么情况下调用精修模型，什么情况下直接使用确定性规则，以及出现异常时如何判断原因。

## 一、当前处理顺序

每个 ASR 片段大致经过以下流程：

```text
ASR 原文
  ↓
实体保护与实体候选准备
  ↓
确定性数字规范化（金额、日期、百分比、计量单位）
  ↓
门控判断：调用精修模型，或直接跳过
  ↓
精修模型输出
  ↓
占位符、内容完整性、数字值校验
  ↓
失败时回退到规则处理后的原文
  ↓
最终数字/实体规则规范化
```

数字规则和实体规则不依赖精修模型；门控只负责决定是否调用模型，不会关闭规则处理。

## 二、什么时候会使用精修模型

当前服务使用：

```text
--refinement-gate-mode conservative
```

在 `conservative` 模式下，满足以下任一条件时会调用精修模型：

| 条件 | 原因 |
| --- | --- |
| 检测到自我纠正 | 例如“苹果，不对，是梨” |
| 检测到重复或语气词 | 例如“你好你好”“嗯、呃、这个” |
| 检测到需要数字规范化的原文 | 仅当门控判断时仍能看到中文数字语境 |
| ASR 置信度低于 0.92 | 低置信度文本需要模型辅助修正 |
| ASR 置信度缺失 | 缺少置信度时默认放行到模型 |
| 句子不完整 | 片段尚未以句号、问号等结束 |
| 存在实体候选或实体提示 | 需要模型结合上下文确认实体用法 |

模型主要负责：

- 删除重复和口癖；
- 处理自我纠正；
- 修正上下文相关错字；
- 补充必要标点；
- 在安全范围内整理句子。

模型不是数字转换的唯一来源，也不能直接信任模型输出的数字。

## 三、什么时候跳过精修模型

在 `conservative` 模式下，满足以下全部条件时会跳过模型：

- 片段完整，以句末标点结束；
- ASR 置信度不低于 0.92；
- 没有检测到重复、语气词、自我纠正等清理信号；
- 没有待确认的实体候选；
- 片段不是空文本。

另外，长度不超过 2 个可见字符的干净片段也会跳过模型。

跳过模型不等于完全不处理。跳过后仍会执行：

- 已验证实体的规范化；
- 确定性中文数字规范化；
- 最终的实体审计。

例如：

```text
原文：今天是二零一五年五月五日。
处理：跳过精修模型，但规则模块仍转换为 2015年5月5日。
```

## 四、什么时候使用规则精修

### 1. 中文数字规范化

规则模块优先处理明确的数字场景：

```text
二万二千二百元       → 22200元
两千一百三十五元     → 2135元
百分之五             → 5%
二零一五年十二月五日 → 2015年12月5日
一到两点五米         → 1到2.5米
```

规则模块不会看到中文数字就盲目转换。没有明确数字单位或数字语境时保持原文，例如：

```text
一五一十
一心一意
七上八下
一个苹果
```

这些表达属于普通语言或固定成语，不应被转换成阿拉伯数字。

### 2. 已验证实体规范化

实体数据库中的标准名称和别名可以由规则模块直接保护或替换。例如：

```text
厉飞宇 → 厉飞羽
鬼灵门 → 鬼灵门（保持标准名称）
```

当前服务使用 `--entity-fuzzy-mode auto`，但高风险实体不会仅凭模糊相似度自动替换。流式中间和最终窗口现在统一允许通过阈值的低风险实体规范化；如果 ASR 后续改写 source span，窗口缓存会重新匹配。

### 3. 规则保护与数字规范化不是同一个开关

当前启动命令包含：

```text
--disable-rule-protection
```

这只关闭 URL、邮箱、日期、时间、阿拉伯数字、标识符等自动占位符保护；数据库实体仍然可以保护。

数字规范化仍然开启，因为它由独立模块负责。只有显式添加下面的参数，才会关闭中文数字规范化：

```text
--disable-numeric-normalization
```

## 五、模型输出异常时如何处理

精修模型输出不会直接进入最终文本，而是经过以下校验：

- 占位符是否缺失、重复或顺序改变；
- 是否出现严重内容丢失；
- 数字的实际数值是否改变；
- 是否出现重复生成或截断。

例如模型生成：

```text
二万二千二百元 → 22000元
```

数字值校验会拒绝该结果，并回退到规则处理后的原文，最终得到：

```text
22200元
```

自我纠正会使用特殊的尾句判断：

```text
我有一个苹果，我有一个梨，不对，我有一个香蕉，我有2135元。
→ 我有一个香蕉，我有2135元。
```

`不对`、`不是`、`而是`、`应该是` 后面的当前纠正分句遇到逗号、顿号或句末标点时结束。只要模型保留了纠正结果且没有压缩掉过多上下文，就不会被误判为严重内容丢失。

## 六、如何看日志判断本次走了哪条路径

重点查看 JSONL 中的以下字段：

| 字段 | 含义 |
| --- | --- |
| `refinement_gate_mode` | 当前门控模式 |
| `refinement_gate_decisions` | 每个片段是 `skip` 还是 `refine` |
| `refinement_gate_skipped_segments` | 跳过模型的片段数 |
| `refiner_executed` | 本次是否调用过模型 |
| `refiner_masked_outputs` | 模型实际生成的候选结果 |
| `refiner_retry_count` | 完整性失败后的重试次数 |
| `refiner_reject_reasons` | 模型结果被拒绝的原因 |
| `numeric_normalizations` | 数字规则实际做过的转换 |
| `entity_normalizations` | 实体规则实际做过的转换 |
| `refiner_accepted` | 所有模型片段是否都通过校验 |

典型判断：

```text
refiner_executed=false
refinement_gate_skipped_segments > 0
```

表示本次主要由门控和规则处理完成，没有调用模型。

```text
refiner_executed=true
refiner_accepted=true
```

表示至少调用了一次模型，并且模型结果通过校验。

```text
refiner_executed=true
refiner_accepted=false
```

表示模型调用过，但至少一个片段被拒绝并回退。此时应结合 `refiner_reject_reasons` 判断是内容丢失、数字错误、占位符错误还是超时。

## 七、实验对比注意事项

原始全量精修基线：

```bash
--refinement-gate-mode off
```

启用保守门控：

```bash
--refinement-gate-mode conservative
```

关闭确定性数字规范化做 A/B 对比：

```bash
--disable-numeric-normalization
```

建议每次实验记录以下指标：

- 模型调用次数；
- `refinement_gate_skipped_segments`；
- 精修总延迟；
- `refiner_accepted` 比例；
- 数字规范化准确率；
- 自我纠正保留率；
- 语气词和重复内容删除率；
- 内容损失回退次数。

不要只根据最终文本是否“看起来通顺”判断效果。数字错误可能表面通顺，因此必须单独统计数字值是否保持一致。

## 八、项目启动开关总表

下面按当前源码中的主要入口整理开关。路径参数和位置参数也列出，便于复现实验。

### 8.1 当前浏览器 Web 服务：`system.web_app`

当前 `启动指令` 使用的关键配置是：Qwen ASR 在 `8766` 端口，Web/精修服务在 `8081` 端口，精修模型使用 `cuda:0`。

| 开关 | 默认值 | 作用 |
| --- | --- | --- |
| `--refiner-model PATH` | 必填 | AgenticASR 精修模型目录 |
| `--refiner-device DEVICE` | `cuda:1` | 精修模型设备；当前启动指令为 `cuda:0` |
| `--asr-url URL` | `http://127.0.0.1:8766` | 流式 ASR 服务地址 |
| `--language LANG` | `Chinese` | ASR 语言提示；`auto` 表示自动检测 |
| `--max-new-tokens N` | `256` | 每次模型精修的最大生成 token 数 |
| `--host HOST` | `127.0.0.1` | Web 服务监听地址 |
| `--port PORT` | `8081` | Web 服务监听端口 |
| `--output PATH` | 不写文件 | 将在线转录结果追加写入 JSONL |
| `--entity-db PATH` | 不启用数据库 | 启用 SQLite 实体库；数据库实体仍可保护 |
| `--entity-fuzzy-mode MODE` | `shadow` | `off`、`shadow`、`hint`、`auto`；当前使用 `auto` |
| `--refinement-gate-mode MODE` | `off` | `off` 全量调用模型；`conservative` 启用保守门控 |
| `--disable-rule-protection` | 不加 | 关闭 URL、邮箱、日期、时间、数字、标识符等自动规则占位符保护 |
| `--disable-numeric-normalization` | 不加 | 关闭确定性中文数字规范化；默认开启 |

常用实验组合：

```bash
# 当前推荐配置
python -m system.web_app \
  --refiner-model /path/to/AgenticASR-Refiner \
  --asr-url http://127.0.0.1:8766 \
  --entity-db data/entities.db \
  --entity-fuzzy-mode auto \
  --refinement-gate-mode conservative \
  --output results/web/session.jsonl

# 门控 A/B 基线：其他配置不变，只关闭门控
python -m system.web_app \
  --refiner-model /path/to/AgenticASR-Refiner \
  --asr-url http://127.0.0.1:8766 \
  --refinement-gate-mode off

# 数字模块 A/B 基线
python -m system.web_app \
  --refiner-model /path/to/AgenticASR-Refiner \
  --asr-url http://127.0.0.1:8766 \
  --disable-numeric-normalization
```

### 8.2 Qwen 流式 ASR：`system.qwen_asr_stream_server`

| 开关 | 默认值 | 作用 |
| --- | --- | --- |
| `--model PATH` | 必填 | Qwen3-ASR 模型目录 |
| `--host HOST` | `127.0.0.1` | ASR 服务监听地址 |
| `--port PORT` | `8766` | 流式 ASR 端口 |
| `--gpu-memory-utilization FLOAT` | `0.75` | vLLM GPU 显存占用上限；当前为 `0.55` |
| `--max-model-len N` | `32768` | ASR 上下文长度 |
| `--chunk-size-seconds FLOAT` | `1.0` | 音频块大小 |
| `--unfixed-chunk-num N` | `4` | 保留的未固定音频块数 |
| `--unfixed-token-num N` | `5` | 保留的未固定 token 数 |
| `--vad MODE` | `silero` | `off`、`silero`、`energy`、`firered` |
| `--vad-model PATH` | `models/silero_vad.onnx` | Silero VAD 模型 |
| `--firered-dir PATH` | `models/firered_vad` | FireRedVAD 模型目录 |
| `--vad-provider PROVIDER` | `cpu` | Silero/FireRed 的 sherpa-onnx provider |
| `--vad-threshold FLOAT` | `0.5` | VAD 语音概率阈值 |
| `--vad-min-silence FLOAT` | `0.7` | 结束语音所需静音秒数 |
| `--vad-min-speech FLOAT` | `0.25` | 开始语音所需最短语音秒数 |
| `--vad-energy-threshold FLOAT` | `0.02` | `energy` VAD 的 RMS 阈值 |
| `--vad-preroll FLOAT` | `0.35` | VAD 开始前保留的音频秒数 |
| `--silence-rms-threshold FLOAT` | `0.002` | 静音 RMS 兜底；设置为 `0` 关闭 |
| `--confidence-logprobs N` | `0` | 采集 token 置信度候选数；当前为 `5` |
| `--confidence-temperature FLOAT` | `1.0` | 将 logprob 转成置信度时的温度 |
| `--confidence-calibrated` | 不加 | 声明置信度已用开发集校准；没有校准时不要添加 |
| `--segment-seconds FLOAT` | `30.0` | 达到该时长后优先重建 ASR 状态 |
| `--max-segment-seconds FLOAT` | `45.0` | 达到该时长后强制重建 ASR 状态 |

门控依赖 ASR 置信度时，建议保留 `--confidence-logprobs 5`。置信度只是门控输入，不代表模型输出一定正确；数字仍需经过数值校验。

### 8.3 麦克风客户端：`system.live_qwen_refiner`

| 开关 | 默认值 | 作用 |
| --- | --- | --- |
| `--refiner-model PATH` | 非列设备模式必填 | 精修模型目录 |
| `--refiner-device DEVICE` | `cuda:1` | 精修模型设备 |
| `--asr-url URL` | `http://127.0.0.1:8765/transcribe` | 非流式 Qwen ASR HTTP 地址 |
| `--asr-timeout FLOAT` | `120` | ASR 请求超时秒数 |
| `--device-index N` | 系统默认 | 麦克风设备编号 |
| `--list-devices` | 不加 | 列出麦克风后退出 |
| `--language LANG` | `Chinese` | ASR 语言提示；`auto` 自动检测 |
| `--vad-threshold FLOAT` | `0.02` | 本地能量 VAD RMS 阈值 |
| `--vad-min-silence FLOAT` | `0.7` | 结束语音所需静音秒数 |
| `--vad-min-speech FLOAT` | `0.25` | 开始语音所需最短语音秒数 |
| `--preroll FLOAT` | `0.5` | 语音开始前保留秒数 |
| `--refiner-max-new-tokens N` | `256` | 精修最大生成 token 数 |
| `--output PATH` | 不写文件 | 保存麦克风转录 JSONL |
| `--entity-db PATH` | 不启用数据库 | 实体数据库 |
| `--entity-domain DOMAIN` | `general` | 实体领域；同时加载 `general` |
| `--entity-fuzzy-mode MODE` | `shadow` | `off`、`shadow`、`hint`、`auto` |
| `--refinement-gate-mode MODE` | `off` | `off` 或 `conservative` |
| `--disable-rule-protection` | 不加 | 关闭自动规则占位符保护 |
| `--disable-numeric-normalization` | 不加 | 关闭确定性数字规范化 |

### 8.4 旧版本地流式客户端：`system.live_asr`

该入口主要用于 MLX/macOS 或旧版 ASR 后端，不是当前浏览器 Web 路径。

| 开关 | 默认值 | 作用 |
| --- | --- | --- |
| `--asr-dir PATH` | `models/asr` | ASR 模型目录 |
| `--asr-type TYPE` | `auto` | `auto`、`transducer`、`wenet-ctc` |
| `--model-type TYPE` | 空 | 后端模型类型覆盖 |
| `--provider PROVIDER` | macOS ARM 为 `coreml`，否则 `cpu` | ASR 推理 provider |
| `--wav PATH` | 麦克风 | 使用 WAV 文件而不是麦克风 |
| `--device-index N` | 系统默认 | 麦克风设备 |
| `--list-devices` | 不加 | 列出麦克风 |
| `--vad MODE` | `silero` | `silero`、`energy`、`firered` |
| `--vad-model PATH` | `models/silero_vad.onnx` | Silero 模型 |
| `--firered-dir PATH` | `models/firered_vad` | FireRedVAD 目录 |
| `--vad-threshold FLOAT` | `0.5` | VAD 概率阈值 |
| `--vad-min-silence FLOAT` | `0.7` | 结束语音静音时长 |
| `--vad-min-speech FLOAT` | `0.25` | 最短语音时长 |
| `--energy-threshold FLOAT` | `0.02` | 能量 VAD 阈值 |
| `--tail-pad FLOAT` | `1.0` | 语音尾部补齐秒数 |
| `--preroll FLOAT` | `0.7` | 语音起始预留秒数 |
| `--max-chunk-chars N` | `80` | 单个精修窗口最大字符数 |
| `--window-size N` | `3` | 滑动窗口最多句块数 |
| `--refiner PATH` | 无 | MLX Refiner 模型目录 |
| `--identity-refiner` | 不加 | 仅输出 ASR/分块，跳过精修模型 |
| `--max-new-tokens N` | `512` | MLX 精修最大 token 数 |
| `--no-cjk-normalize` | 不加 | 关闭中日韩字符归一化 |

`--refiner` 与 `--identity-refiner` 互斥；后者只适合 ASR 和分块诊断。

### 8.5 非流式离线脚本

`experiments/scripts/transcribe_and_refine.py`：

| 开关 | 默认值 | 作用 |
| --- | --- | --- |
| `audio` | 必填位置参数 | 输入音频 |
| `output_jsonl` | `results/e2e/result.jsonl` | 输出 JSONL |
| `--asr-backend` | `qwen3` | `qwen3` 或 `whisper` |
| `--asr-model` | 必填 | ASR 模型 |
| `--refiner-model` | 必填 | 精修模型 |
| `--asr-env` | 自动选择 | ASR Conda 环境 |
| `--refiner-env` | `agentic-asr` | 精修 Conda 环境 |
| `--device` | `cuda:0` | 推理设备 |
| `--language` | `Chinese` | 语言提示 |
| `--source-record-id` | `audio-001` | 记录 ID |
| `--asr-max-new-tokens` | `256` | ASR 生成上限 |
| `--refiner-max-new-tokens` | `1024` | 精修生成上限 |
| `--json-output` | 自动生成 | 格式化 JSON 输出路径 |
| `--entity-db PATH` | 不启用 | 实体数据库 |

`experiments/scripts/transcribe_and_refine_qwen3.py` 的额外开关为：
`--qwen-env`（默认 `qwen3-asr`）、`--refiner-env`（默认
`agentic-asr`）、`--device`、`--language`、`--source-record-id`、
`--asr-max-new-tokens`、`--refiner-max-new-tokens` 和 `--json-output`。

### 8.6 原始 ASR 服务入口

`system.qwen_asr_server`（单次 HTTP ASR）：

```text
--model PATH       必填，Qwen3-ASR 模型
--device DEVICE    默认 cuda:0
--host HOST        默认 127.0.0.1
--port PORT        默认 8765
--max-new-tokens N 默认 256
```

`system.whisper_stream_server`（Whisper 滚动窗口服务）：

```text
--model PATH                 必填
--device DEVICE              默认 cuda:0
--dtype DTYPE                默认 auto，可选 float16/bfloat16 等
--language LANG              默认 Chinese，auto 自动检测
--max-new-tokens N           默认 256
--update-interval-seconds T  默认 1.5
--host HOST                  默认 127.0.0.1
--port PORT                  默认 8766
```

### 8.7 后处理、校准和实体库管理

`experiments/scripts/postprocess_asr.py`：

```text
input_jsonl output_jsonl     必填位置参数
--model PATH                 后处理模型，必填
--batch-size N               默认 8
--max-new-tokens N           默认 512
--device-map MAP             默认 auto
--dtype DTYPE                默认 auto
--temperature FLOAT          默认 0.7
--top-p FLOAT                默认 0.8
--do-sample                  启用采样
--trust-remote-code          允许远程模型代码
--limit N                    只处理前 N 条
--overwrite                  允许覆盖输出
--validate-only              只验证，不生成
--entity-db PATH             实体数据库
--entity-domain DOMAIN       默认 general
--entity-fuzzy-mode MODE     默认 shadow
--refinement-gate-mode MODE  默认 off
```

`experiments/scripts/calibrate_confidence.py`：

```text
input output                  必填位置参数
--score-field FIELD           默认 asr_confidence
--label-field FIELD           默认 confidence_label
```

`system.manage_entities`：

```text
--db PATH                     必填 SQLite 数据库
add CANONICAL                 添加实体
  --alias TEXT                可重复添加别名
  --type TYPE                 默认 TERM
  --domain DOMAIN             默认 general
  --policy POLICY             preserve 或 normalize，默认 preserve
  --priority N                默认 0
list
  --domain DOMAIN             按领域筛选
  --all                       包含禁用实体
enable/disable CANONICAL
  --domain DOMAIN             默认 general
```

`experiments/scripts/main.py`（Gemma 评测器）：

```text
asr_jsonl                    必填，待评测 JSONL
--rubric PATH                必填，评测 rubric
--results PATH               评测明细输出
--summary PATH               汇总输出
--text-field FIELD           指定待评测文本字段
--api-url URL                默认 GEMMA_API_URL 或 http://127.0.0.1:8000/v1
--api-key KEY                默认 GEMMA_API_KEY
--model MODEL                默认 GEMMA_MODEL 或项目默认模型
--workers N                  默认 GEMMA_JUDGE_WORKERS 或 4
--timeout FLOAT              默认 GEMMA_TIMEOUT 或 120 秒
--attempts N                 默认 GEMMA_ATTEMPTS 或 3
--limit N                    只评测前 N 条
--overwrite                  允许覆盖已有评测结果
--validate-only              只验证输入和输出，不调用评测模型
```

这些环境变量只影响评测器，不影响 ASR、门控或 Web 精修服务：
`GEMMA_API_URL`、`GEMMA_API_KEY`、`GEMMA_MODEL`、`GEMMA_JUDGE_WORKERS`、
`GEMMA_TIMEOUT`、`GEMMA_ATTEMPTS`。

### 8.8 环境变量和端口注意事项

- `CUDA_VISIBLE_DEVICES` 决定进程可见 GPU；它不是 Python 参数，但会影响 `cuda:0` 实际对应哪张物理卡。
- 当前启动指令让 ASR 使用 GPU 0、Web 精修进程使用 GPU 1，并在 Web 进程内使用 `--refiner-device cuda:0`。
- Qwen 流式 ASR 默认端口为 `8766`，Web 服务默认端口为 `8081`，旧版单次 Qwen 服务默认端口为 `8765`。
- 修改 ASR 端口后，必须同步修改 Web 的 `--asr-url`。
- 修改 `--refinement-gate-mode`、`--disable-numeric-normalization` 或实体匹配开关后，需要重启 Web/精修服务；刷新浏览器本身不会重新加载 Python 参数。
